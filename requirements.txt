opencv-contrib-python>=4.7
numpy>=1.23
Pillow>=10.0
reportlab>=3.6
# ps3eye_tools depends on a locally-built `ps3eye` module from
# https://github.com/soswow/PS3Eye-library (see `ps3eye_tools/README.md`).
